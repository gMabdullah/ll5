#
- Download and install the composer (for window user composer exe is available)
- move it to your desire directory, to access it my simply write "composer" by this command
"mv composer.phar /usr/local/bin/composer"
know you can access it globally
check it by writting composer and hit enter <-

#After the dwnld the require packages by laravel in order to work with
-composer require phpspec/phpspec
-composer create-project laravel/laravel ll5

#To star server run that
php -S localhost:8000 -t public

#To create composer
php artisan make:controller PagesController
// if you need help write like that
// php artisan help make:controller
// that will show you also the possible flags
 